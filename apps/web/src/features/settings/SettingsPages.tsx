import { ResourcePage } from "@/features/resource/ResourcePage";
export function IntegrationsPage() { return <ResourcePage title="Integrations" description="Connect future maps, notification, payment, and analytics providers." collection="approvals" primaryAction="Add integration" view="settings" />; }
